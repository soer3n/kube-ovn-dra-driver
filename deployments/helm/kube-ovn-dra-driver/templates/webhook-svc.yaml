{{- if .Values.webhook.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-ovn-dra-driver.fullname" . }}-webhook
  namespace: {{ include "kube-ovn-dra-driver.namespace" . }}
  labels:
    {{- include "kube-ovn-dra-driver.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  selector:
    {{- include "kube-ovn-dra-driver.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
  ports:
  - name: webhook
    protocol: TCP
    port: {{ .Values.webhook.servicePort }}
    targetPort: webhook
{{- end }}
