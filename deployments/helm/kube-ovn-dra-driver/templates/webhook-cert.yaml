{{- if .Values.webhook.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "kube-ovn-dra-driver.fullname" . }}-webhook-cert
  namespace: {{ include "kube-ovn-dra-driver.namespace" . }}
  labels:
    {{- include "kube-ovn-dra-driver.labels" . | nindent 4 }}
    app.kubernetes.io/component: webhook
spec:
  dnsNames:
    {{- $svcName := printf "%s-webhook" (include "kube-ovn-dra-driver.fullname" .) }}
    {{- $svcNamespace := (include "kube-ovn-dra-driver.namespace" .) }}
    - {{ $svcName }}.{{ $svcNamespace }}.svc
  issuerRef:
    kind: Issuer
    name: {{ include "kube-ovn-dra-driver.fullname" . }}-webhook-issuer
  secretName: {{ include "kube-ovn-dra-driver.fullname" . }}-webhook-cert
{{- end }}
